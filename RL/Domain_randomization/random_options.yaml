world configs:
  - default: ADF/world/world_stereo_varylight.yaml

PSM1 configs:
  -  defaultPSM1: ADF/psm1.yaml

PSM2 configs:
  - defaultPSM2: ADF/psm2.yaml

phantom configs:
  - defaultPhantom: ADF/phantom_without_thread_multiView.yaml